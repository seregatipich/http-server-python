[constraints]
min_quality = 0.60
min_modularity = 0.30
min_acyclicity = 1.0
min_depth = 0.50
min_equality = 0.50
min_redundancy = 0.95
max_cycles = 0
max_cc = 15
max_fn_lines = 100
max_file_lines = 500
max_upward_violations = 0
no_god_files = true

[[layers]]
name = "domain"
paths = ["src/pyhttpd/domain/**"]
order = 3

[[layers]]
name = "application"
paths = ["src/pyhttpd/application/**"]
order = 2

[[layers]]
name = "adapters"
paths = ["src/pyhttpd/adapters/**"]
order = 1

[[layers]]
name = "legacy"
paths = [
    "src/pyhttpd/bootstrap/**",
    "src/pyhttpd/lifecycle/**",
    "src/pyhttpd/pipeline/**",
    "src/pyhttpd/transport/**",
    "src/pyhttpd/handlers/**",
]
order = 1

[[layers]]
name = "entrypoints"
paths = [
    "src/pyhttpd/composition.py",
    "src/pyhttpd/cli.py",
    "src/pyhttpd/__main__.py",
    "src/pyhttpd/__init__.py",
]
order = 0
